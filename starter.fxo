import from "packages/main" { appName, appVersion, isApp }

function startApp() {
    if (isApp = true) {
        console.log("Starting " + appName + " (Version " + appVersion + ")")
    }
}

startApp()
